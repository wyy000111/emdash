{
	"name": "@emdash-cms/flue-review",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "flue dev --target cloudflare",
		"build": "flue build --target cloudflare",
		"deploy": "flue build --target cloudflare && wrangler deploy",
		"cf-typegen": "wrangler types",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@flue/cli": "1.0.0-beta.1",
		"typescript": "catalog:",
		"wrangler": "catalog:"
	},
	"dependencies": {
		"@cloudflare/sandbox": "^0.12.1",
		"@flue/runtime": "1.0.0-beta.1",
		"agents": "^0.14.5",
		"hono": "^4.12.23",
		"valibot": "^1.4.1"
	}
}
